[project]
name = "arxiv2scribe"
version = "1.5.0"
requires-python = ">=3.13"
dependencies = [
    "lxml",
    "requests",
    "typer",
    "wget",
]

[project.scripts]
arxiv2scribe = "arxiv2scribe:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
